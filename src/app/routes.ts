export const publicRoutes = ['/'];

export const protectedRoutes = ['/d', '/a', '/s'];

export const authRoutes = ['/login', '/register'];

export const authPrefix = '/api/auth';

export const DEFAULT_LOGIN_REDIRECT = '/d';
